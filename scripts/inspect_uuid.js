const id = '9dd2fc0a-043b-49a4-94cb-e4e1fae412d9';
console.log(`ID: "${id}"`);
console.log(`Length: ${id.length}`);
for (let i = 0; i < id.length; i++) {
    console.log(`${i}: ${id[i]} (${id.charCodeAt(i)})`);
}
